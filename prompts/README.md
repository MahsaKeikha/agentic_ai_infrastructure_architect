# Prompt Boundary

The offline L3 reference path is deterministic and does not require an LLM. If an LLM adapter is introduced, prompts must preserve the typed state contract, cite the evidence supplied to the model, distinguish assumptions from facts, surface conflicts and unresolved questions, and never convert a human-approval requirement into model authorization.
